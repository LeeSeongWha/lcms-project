<%--
 * == 개정이력(Modification Information) ==
 * 수정일		수정자	수정내용
 * ========================================
 * 250722	김태수	최초 생성
--%>
<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<%@ taglib uri="jakarta.tags.core" prefix="c"%>

<title>LCMS 강의평가 폼 관리</title>
<link rel="stylesheet" href="/dist/assets/css/bodyFormat.css">


<div class="container">
	<form> 
		<input type="hidden" name="enrollNo" value="${enrollNo}">
			<div class="form-section">
				<h2>강의평가</h2> 
				<div class="card">
					<c:forEach items="${evalDetails}" var="detail" varStatus="status">
					    <div class="form-group" style="flex: 1;">
							<div id="questionList">
								<div class="question-item">
									<label class="inputLabel">강의평가 ${status.count}.</label>
									<input class="inputField" type="hidden" name="criteriaNo_${status.count}" value="${detail.criteriaNo}"> 
									<input class="inputField" style="width:70%;" type="text" value="${detail.criteriaDesc}" readonly>
									
									<input type="radio" name="score_${status.count}" value="1" ${detail.evalScore == 1 ? 'checked' : ''} disabled>매우 불만족&nbsp&nbsp
									<input type="radio" name="score_${status.count}" value="2" ${detail.evalScore == 2 ? 'checked' : ''} disabled>불만족&nbsp&nbsp
									<input type="radio" name="score_${status.count}" value="3" ${detail.evalScore == 3 ? 'checked' : ''} disabled>보통&nbsp&nbsp
									<input type="radio" name="score_${status.count}" value="4" ${detail.evalScore == 4 ? 'checked' : ''} disabled>만족&nbsp&nbsp
									<input type="radio" name="score_${status.count}" value="5" ${detail.evalScore == 5 ? 'checked' : ''} disabled>매우 만족
								</div>
							</div>
						</div>
					</c:forEach>
				</div>
				<label class="inputLabel">자유 의견</label>
				<textarea class="textareaField" name="comment" rows="5" cols="50" readonly>${evalComment}</textarea>
			</div>
			<button type="button" class="cancelButton" onclick="history.back()"  style="float:right; margin-right:20px;">이전</button>
	</form>
</div>