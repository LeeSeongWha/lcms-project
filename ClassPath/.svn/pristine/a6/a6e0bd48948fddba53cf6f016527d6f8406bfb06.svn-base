@charset "UTF-8";

@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700&display=swap');

/* 기존 enrollMain2.css의 기본 설정 일부를 가져와 통일감을 줍니다. */
body {
    font-family: 'Noto Sans KR', sans-serif;
    justify-content: center;
    box-sizing: border-box;
    /* 만약 body에 전체적인 배경색이 있다면 여기에 추가하세요 (예: background-color: #f0f2f5; ) */
}


/* 컨테이너 역할을 하는 div (전체적인 배경, 그림자 등) */
.grade-container { /* 기존 container와 이름 충돌을 피하기 위해 grade-container로 변경 */
    margin: 20px auto; /* 상하 여백, 좌우 자동 (중앙 정렬) */
    position: relative;
    border-radius: 15px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    color: #333; /* 기본 텍스트 색상을 어둡게 조정 */
    padding: 30px; /* 내부 여백 */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 30px; /* 섹션 간의 기본 간격 */
    width: 90%;
    max-width: 1200px;
}

/* 폼 그룹 및 필터 스타일 (기존 enrollMain2.css와 통일) */
.form-row {
    display: flex;
    gap: 20px; /* 1.25rem = 20px */
    flex-wrap: wrap; /* 작은 화면에서 줄바꿈 */
}

.form-group {
    flex: 1; /* flex:1; 적용 */
    min-width: 150px; /* 최소 너비 설정 (콘텐츠에 따라 조정) */
    display: flex;
    flex-direction: column;
    margin-bottom: 0; /* form-row가 gap을 가지므로 개별 margin-bottom 제거 */
}

.inputLabel {
    font-size: 0.9em;
    color: #555; /* 라벨 글자색 어둡게 */
    margin-bottom: 5px;
    display: block;
}

.inputField,
.selectBox {
    width: 100%;
    padding: 10px 15px;
    border-radius: 8px;
    border: 1px solid #ccc; /* 테두리 색상 조정 */
    background-color: #f8f8f8; /* 입력 필드 배경색 밝게 조정 */
    color: #333; /* 입력 필드 텍스트 색상 어둡게 */
    font-size: 1em;
    box-sizing: border-box; /* 패딩이 너비에 포함 */
}

.inputField::placeholder,
.selectBox::placeholder {
    color: #888; /* 플레이스홀더 색상 어둡게 */
}

.inputField:focus,
.selectBox:focus {
    outline: none;
    border-color: #88ccee; /* 포커스 시 밝은 파랑 */
    box-shadow: 0 0 0 3px rgba(136, 204, 238, 0.4);
}

/* 체크박스 및 라벨 스타일 */
input[type="checkbox"].checkbox {
    margin-top: 10px; /* HTML에 인라인 스타일로 적용된 것 유지 */
    margin-right: 5px; /* 체크박스와 텍스트 사이 간격 */
    transform: scale(1.2); /* 체크박스 크기 약간 키움 */
    cursor: pointer;
}

.form-group > label { /* 체크박스 옆 '전체 성적' 텍스트에 적용될 라벨 */
    display: inline-block; /* 체크박스와 같은 라인에 오도록 */
    color: #555; /* 글자색 통일 */
    font-size: 0.9em;
    margin-top: 10px; /* 체크박스와 동일한 수직 정렬을 위해 */
}


/* 검색 버튼 스타일 (기존 submitButton 스타일 재활용) */
/* 기존 gradeMain.css (혹은 enrollMain2.css)에서 .submitButton 부분을 찾아서 아래와 같이 변경합니다. */

/* 검색 버튼 스타일 (수강신청 페이지의 파란색 버튼과 통일) */
.submitButton {
    background-color: #007bff; /* 파란색 계열 */
    color: white;
    border: none;
    border-radius: 8px;
    padding: 10px 20px;
    font-size: 1em;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
    white-space: nowrap;
    width: 200px; /* HTML에 인라인 스타일로 적용된 것 유지 */
    margin-top: 20px; /* 검색 버튼 위 간격 */
}

.submitButton:hover {
    background-color: #0056b3; /* 호버 시 더 진한 파란색 */
    transform: translateY(-2px);
}

/* 성적 정보 표시 영역 (pointAvg) */
#pointAvg {
    background: linear-gradient(to right, #4A76FF, #8E44AD);
    border-radius: 10px;
    padding: 10px;
    box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
    color: #333; /* 텍스트 색상 어둡게 */
    margin-bottom: 20px; /* 아래 테이블과의 간격 */
    text-align: center; /* 내용이 중앙에 오도록 */
    font-size: 1.1em;
    font-weight: 500;
    color:white;
}
/* pointAvg 내부의 실제 성적 값이 들어갈 요소에 대한 스타일 (예상) */
#pointAvg strong {
    font-size: 1.8em;
    color: #4A76FF; /* 강조 색상 */
    margin-right: 5px;
}
#pointAvg span {
    color: #666;
    font-size: 0.9em;
}


/* 테이블 스타일 (기존 defaultTable에서 가져와 적용) */
.defaultTable { /* class="defaultTable" */
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px; /* 위 div와의 간격 */
    background-color: #F8FCFF; /* 테이블 배경을 매우 밝은 하늘색으로 변경 (최신 변경사항 반영) */
    border-radius: 8px;
    overflow: hidden; /* 라운딩 적용을 위해 */
    table-layout: fixed; /* 셀 가로 길이 통일을 위해 추가 */
}

.tableHead { /* class="tableHead" */
    /* thead에 직접 배경색을 주어 th에 각각 주는 것을 대체 */
    background-color: #e9ecef; /* 헤더 배경을 밝은 회색으로 변경 */
}

.tableTh, .tableTd { /* class="tableTh", class="tableTd" */
    padding: 12px 15px;
    border-bottom: 1px solid #eee; /* 테두리 색상 조정 */
    color: #333; /* 텍스트 색상 어둡게 조정 */
    font-size: 0.95em;
    box-sizing: border-box; /* 패딩이 너비에 포함되도록 */
}

.tableTh {
    font-weight: 600;
    white-space: nowrap; /* 헤더 텍스트 줄바꿈 방지 */
    /* th 너비는 table-layout: fixed; 때문에 td와 동일하게 적용됨 */
    width: calc(100% / 6); /* 6개의 컬럼 (학기, 강의, 이수구분, 학점, 등급, 평점) */
}

.defaultTable tbody tr:last-child .tableTd {
    border-bottom: none; /* 마지막 행의 하단 테두리 제거 */
}

.defaultTable tbody tr:hover {
    background-color: #EBF5FB;
}

/* 반응형 디자인 (기존 enrollMain2.css에서 가져와 조정) */
@media (max-width: 992px) {
    .grade-container {
        padding: 20px;
        gap: 20px;
    }

    .form-row {
        flex-direction: column; /* 필터 컬럼 세로로 쌓이도록 */
        gap: 10px;
    }

    .form-group {
        width: 100%;
        justify-content: flex-end;
    }

    .submitButton {
        width: 100%; /* 버튼 너비 100%로 변경 */
        margin-top: 15px;
    }

    .tableTh, .tableTd {
        padding: 10px;
        font-size: 0.9em;
    }

    /* 모바일에서 테이블 스크롤 가능하게 (필요시) */
    .tableContainer {
        overflow-x: auto; /* 가로 스크롤 가능하게 */
    }
    .defaultTable {
        min-width: 600px; /* 테이블이 너무 좁아지지 않도록 최소 너비 설정 */
    }
}

@media (max-width: 576px) {
    .grade-container {
        margin: 20px auto;
        padding: 15px;
        gap: 15px;
    }

    .submitButton {
        padding: 8px 15px;
        font-size: 0.9em;
    }
    
    .tableTh, .tableTd {
        padding: 8px;
        font-size: 0.85em;
    }
    
    .defaultTable {
        min-width: 500px; /* 더 작은 화면을 위해 최소 너비 조정 */
    }
    
    #pointAvg {
        padding: 15px;
        font-size: 1em;
    }
    #pointAvg strong {
        font-size: 1.5em;
    }
    #pointAvg span {
        font-size: 0.8em;
    }
}